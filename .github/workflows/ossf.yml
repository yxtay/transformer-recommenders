name: ossf
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    branches: [main]
permissions:
  contents: read
  id-token: write
  security-events: write
jobs:
  ossf:
    uses: yxtay/.github/.github/workflows/ossf.yml@main
