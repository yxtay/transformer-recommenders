name: scans
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    branches: [main]
permissions: {}
jobs:
  megalinter:
    permissions:
      contents: write
      pull-requests: write
      security-events: write
      statuses: write
    uses: yxtay/.github/.github/workflows/scans.yml@92f72e514298502cb5fc27ce1dfe3a79dd21d14a # main
    with:
      run-ci_light: false
      run-python: true
